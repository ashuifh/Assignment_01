// Legacy shim — prefer src/controllers/assessmentController.js (MVC Controller layer)
// Kept for backward compatibility; new code should import from controllers.
export { useAssessment, useAssessmentController } from '../controllers/assessmentController'
